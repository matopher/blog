'use strict';

var worker_threads = require('worker_threads');
var getCliConfig = require('../_chunks/getCliConfig-DoiVfP79.js');
getCliConfig.getCliConfig(worker_threads.workerData, {
  forked: false
}).then(config => {
  var _worker_threads$paren;
  return (_worker_threads$paren = worker_threads.parentPort) === null || _worker_threads$paren === void 0 ? void 0 : _worker_threads$paren.postMessage({
    type: "config",
    config
  });
}).catch(error => {
  var _worker_threads$paren2;
  return (_worker_threads$paren2 = worker_threads.parentPort) === null || _worker_threads$paren2 === void 0 ? void 0 : _worker_threads$paren2.postMessage({
    type: "error",
    error: error instanceof Error ? error.stack : error,
    errorType: error && (error.type || error.name)
  });
});
//# sourceMappingURL=getCliConfig.js.map
